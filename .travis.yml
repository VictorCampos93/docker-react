sudo: required
services:
  - docker

before_install:
  - docker build -t victorcampos/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true victorcampos/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "docker"
  env: "Docker-env"
  bucket_name: "elasticbeanstalk-us-east-1-812942127535"
  bucket_path: "docker"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_key_id: 
    secure: "$AWS_SECRET_KEY"